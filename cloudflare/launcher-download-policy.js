// A Worker entry point may export handlers/classes, not arbitrary values.
// Keep this policy constant importable by tests without exposing it as an entry point.
export const LAUNCHER_INSTALLER_DOWNLOAD_POLICY_EPOCH = '2026-09-04-privacy-reset-1';
